#include "headers/buffer_manager.hpp"
#include <vector>

struct BufferFrame {
  char page_data[page_size];
  size_t page_id = static_cast<size_t>(-1); // gives the max value of size_t
  bool dirty_bit = false;
  bool pin_count = 0;
};

class buffer_pool {
private:
  // want to have array of these
  std::vector<BufferFrame> frames;

public:
  buffer_pool() {}
};

void buffer_manager::buffer_manager() {}
